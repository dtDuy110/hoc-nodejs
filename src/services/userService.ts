const handleCreateUser = (
    name: string,
    email: string,
    address: string
) => {
    // insert to database

    //return result to client.
    console.log(" >>>. inserted a new user");
}

export { handleCreateUser }